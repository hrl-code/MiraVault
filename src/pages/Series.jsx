import CatalogPage from '@/components/catalog/CatalogPage'

export default function Series() {
  return (
    <CatalogPage
      title="Series"
      description="Series ordenadas por temporadas y episodios a partir de los archivos que ya tienes descargados."
      initialType="series"
    />
  )
}
